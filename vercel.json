{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/admin build",
  "outputDirectory": "artifacts/admin/dist",
  "framework": null,
  "env": {
    "PORT": "3000"
  }
}
